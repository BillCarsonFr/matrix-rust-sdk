[**breaking**] `StickyEventEncryptionInfo` has been renamed to `EventEncryptionInfo` and is no longer gated behind the `unstable-msc4354` feature, so that it can also describe incoming to-device messages. It gained a `senderId` field carrying the cryptographically attested sender, which for a to-device message should be trusted over the `sender` claimed in the event itself.
